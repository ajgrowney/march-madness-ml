{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://andrewgrowney.com/schemas/march-madness-ml/model-manifest.schema.json",
  "title": "March Madness Model Manifest",
  "description": "Manifest describing which reference models, runtime configs, or insight artifacts the website can load, which feature set they require, and how matchup inference should be resolved.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "default_model_id",
    "models"
  ],
  "properties": {
    "default_model_id": {
      "type": "string",
      "minLength": 1,
      "description": "Model identifier the website should load by default for the bracket and team pages."
    },
    "models": {
      "type": "array",
      "minItems": 1,
      "description": "List of website-visible models or runtime strategies.",
      "items": {
        "$ref": "#/$defs/modelEntry"
      }
    }
  },
  "$defs": {
    "modelEntry": {
      "title": "Model Manifest Entry",
      "description": "A model option presented to the website user. This may be heuristic, precomputed, runtime-config, TensorFlow.js, or a precomputed matchup-insights artifact.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "name",
        "type",
        "seasons",
        "feature_set",
        "prediction_format",
        "description"
      ],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Stable model identifier used by the website and downstream artifacts."
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Human-readable name displayed in model selection UIs."
        },
        "type": {
          "type": "string",
          "enum": [
            "heuristic",
            "runtime-config",
            "precomputed-predictions",
            "precomputed-insights",
            "tfjs",
            "sklearn-reference"
          ],
          "description": "High-level implementation class for the model."
        },
        "model_family": {
          "type": "string",
          "enum": [
            "heuristic",
            "logistic-regression",
            "random_forest",
            "random-forest",
            "mlp",
            "keras-dnn",
            "xgboost",
            "nearest-neighbor",
            "matchup-clustering",
            "other"
          ],
          "description": "Optional training-family label used to describe the underlying model implementation for precomputed or runtime-backed models."
        },
        "seasons": {
          "type": "array",
          "minItems": 1,
          "description": "Seasons this model can legally be used against in the website.",
          "items": {
            "type": "integer",
            "minimum": 1985
          }
        },
        "feature_set": {
          "type": ["string", "null"],
          "description": "Feature set required by the model. Null is acceptable for models like coin flip that use no team features."
        },
        "prediction_format": {
          "type": "string",
          "enum": [
            "runtime",
            "runtime-config",
            "precomputed",
            "precomputed-insights",
            "tfjs"
          ],
          "description": "How the website should resolve matchup inference for this model."
        },
        "inference_task": {
          "type": "string",
          "enum": [
            "winner-probability",
            "matchup-insights"
          ],
          "description": "Optional declaration of what the model returns for a matchup. Omit for legacy winner-probability models."
        },
        "artifact_format": {
          "type": "string",
          "enum": [
            "pairwise-winner-probability",
            "pairwise-matchup-insights-v1"
          ],
          "description": "Optional browser-facing artifact format. This lets the site distinguish probability tables from matchup-insight tables without inferring from URLs."
        },
        "description": {
          "type": "string",
          "minLength": 1,
          "description": "Explanation shown in the UI to set user expectations."
        },
        "artifact_version": {
          "type": "string",
          "minLength": 1,
          "description": "Optional semver or pipeline version string for the exported model artifact."
        },
        "training_seasons": {
          "type": "array",
          "minItems": 1,
          "description": "Historical seasons used to fit the model offline.",
          "items": {
            "type": "integer",
            "minimum": 1985
          }
        },
        "mirrored_prediction_strategy": {
          "type": "string",
          "enum": [
            "none",
            "average-forward-reverse",
            "average-forward-reverse-complement"
          ],
          "description": "Optional description of how directional matchup probabilities were reconciled before export."
        },
        "exported_at": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the model artifact was written."
        },
        "model_url": {
          "type": ["string", "null"],
          "format": "uri",
          "description": "Optional URL to a TensorFlow.js model.json or other hosted model artifact."
        },
        "config_url": {
          "type": ["string", "null"],
          "format": "uri",
          "description": "Optional URL to a runtime-config JSON artifact."
        },
        "predictions_url": {
          "type": ["string", "null"],
          "format": "uri",
          "description": "Optional URL to a precomputed season-specific matchup prediction table."
        },
        "matchup_insights_url": {
          "type": ["string", "null"],
          "format": "uri",
          "description": "Optional URL to a precomputed season-specific matchup insights table such as clustering analogs and slot-aware summaries."
        },
        "feature_importance_url": {
          "type": ["string", "null"],
          "format": "uri",
          "description": "Optional URL to a feature-importance artifact for offline-trained models."
        },
        "supports_bracket_autofill": {
          "type": "boolean",
          "description": "Optional flag for whether the website may use this model to auto-advance bracket picks directly. Insight-only models should set this to false."
        },
        "training_feature_aliases": {
          "type": "object",
          "description": "Optional map from training-time feature names to browser-facing feature names when a published artifact used aliases internally.",
          "additionalProperties": {
            "type": "string"
          }
        },
        "oob_score": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Optional out-of-bag score for random-forest style models."
        },
        "is_custom": {
          "type": "boolean",
          "description": "Optional flag indicating that the model was user-created in the browser rather than published from the ML repo."
        }
      }
    }
  }
}